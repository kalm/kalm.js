import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { Server } from '../../../src/components/server.ts';

describe('Provider', () => {
  it('TODO', () => assert.notEqual(Server, undefined));
});
